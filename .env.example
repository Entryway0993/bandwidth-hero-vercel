# ==========================================
# VERCEL ENVIRONMENT VARIABLES
# ==========================================
# Add these to your Vercel Project Settings -> Environment Variables.

# --- Server & Logging ---
PORT=3000
LOG=1

# --- Client Authentication ---
# The key your browser extension or apps will use.
API_KEY=your_secure_api_key_here
LOGIN=
PASSWORD=

# --- Cloudflare Worker Integration ---
# The URL of your deployed Cloudflare Worker (e.g., https://your-worker.workers.dev)
CF_WORKER_URL=
# A secret key only Vercel and the Worker know. Used for the /raw back-channel.
INTERNAL_KEY=your_internal_key_here

# --- Network & Fetch Limits ---
UPSTREAM_ACCEPT_ENCODING=identity
MAX_DOWNLOAD_BYTES=20971520
REQUEST_TIMEOUT_MS=45000

# --- Compression Quality ---
DEFAULT_QUALITY=40
MIN_QUALITY=10
MAX_QUALITY=100

# --- AVIF / WebP Encoding Effort ---
# Lower = faster, Higher = smaller file size but more CPU.
AVIF_EFFORT_SMALL=4
AVIF_EFFORT_MEDIUM=3
AVIF_EFFORT_LARGE=2
WEBP_EFFORT=4

# --- Manga / Manhwa / Image Resizing ---
# Normal images max side cap. 0 disables.
MAX_OUTPUT_DIM=2560
# Long vertical strip width cap. 0 disables.
MAX_STRIP_WIDTH=1600
# true = grayscale by default, false = keep color.
DEFAULT_GRAYSCALE=false

# --- Text Sharpening ---
# off | resized | always
SHARPEN_MODE=resized
# 0.5 (subtle) to 2.0 (aggressive). 0.8 is best for comics.
SHARPEN_SIGMA=0.8

# --- Animation Protection ---
# Max frames to process. If an animation has more, it bypasses compression to prevent OOM.
MAX_ANIMATED_FRAMES=1000

# --- Format Compatibility ---
# true = Automatically negotiate format based on client Accept header.
# false = Force AVIF on everyone (Dictator mode).
ALLOW_ACCEPT_FALLBACK=true


# --- Cloudflare Worker Integration ---
# A secret key only Vercel and the Worker know. Used for the /raw back-channel.
INTERNAL_KEY=your_internal_key_here

# MANDATORY: A dedicated, highly-restricted key for the /purge route.
# DO NOT leave this blank. If blank, the Worker will disable purging entirely 
# to prevent standard users from nuking the cache.
PURGE_KEY=your_dedicated_purge_key_here
